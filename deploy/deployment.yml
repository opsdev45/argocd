apiVersion: v1
kind: Service
metadata:
  name: service-app
spec:
  type: NodePort
  ports:
  - port: 8000
  selector:
    app: weather
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-deployment
spec:
  selector:
    matchLabels:
      app: weather
  replicas: 2
  template:
    metadata:
      labels:
        app: weather
    spec:
      containers:
      - name: weather
        image: devops45of/app:2fd7a8051e4432a92da04f62c06d25560bd3345e
        env:
          - name: BG_COLOR
            value: 'green'


